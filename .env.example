# Frontend (Vite)
# Leave empty to use same-origin + vite proxy in development.
# VITE_API_BASE_URL=http://127.0.0.1:8000

# Optional: proxy target when using relative API paths (default below)
# VITE_PROXY_TARGET=http://127.0.0.1:8000
