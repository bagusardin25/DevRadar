"""Multi-provider LLM routing (failover, rotation, rate limits, usage).

Import from the submodules directly (``app.llm.router``, ``app.llm.registry``)
so this package stays free of import cycles with ``app.config``.
"""
