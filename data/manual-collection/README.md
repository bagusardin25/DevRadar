# Manual collection dumps

- `candidates.example.jsonl` — schema sample (safe to commit).
- `candidates.jsonl` — real curated rows from X MCP (local; optional gitignore).

See `docs/manual-x-collection.md`.
